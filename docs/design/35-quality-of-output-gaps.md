# 35: Quality of Output Gaps — Ensuring Sessions Ship Meaningful Work

> Status: Active | Created: 2026-04-20
> Applies to: all projects using otherness

---

## The problem

An autonomous session that processes only `kind/chore` or `kind/docs` items ships
no user-visible improvements. The queue can drift toward chore-only composition when:

- Design doc `🔲 Future` items are exhausted mid-session
- PM/SM-derived hygiene items accumulate faster than vision items are written
- A session inherits a queue that was already chore-heavy

A session that starts on a chore-only queue is executing in low-value mode. The
agent should recognize this and inject vision-derived items before claiming work.

---

## The queue refusal guard

**Trigger condition**: when all `state=todo` items in the queue have labels in
`{kind/chore, kind/docs}` — and none have `kind/enhancement` or `kind/bug` —
the session must enrich the queue before claiming the next item.

**Enrichment sequence** (from `docs/design/22-queue-richness.md`):
1. Re-scan `docs/design/` for unclaimed `🔲 Future` items
2. If none: pull from earliest incomplete roadmap stage
3. If none: trigger autonomous vision synthesis inline
4. If none after synthesis: log warning, allow chore claim to avoid stalling

The guard fires at `coord.md §1c` (queue generation), not at `§1e` (item claim).
This keeps the claim logic clean and ensures enrichment happens before the session
commits to a specific item.

---

## Present (✅)

- ✅ `coord.md §1c`: queue refusal guard — when all todo items are `kind/chore` or `kind/docs`, trigger enrichment before claiming; enrichment follows design doc → roadmap → vision sequence; posts report comment when triggered (PR #629, 2026-04-20)
- ✅ `SM §4b`: session outcome classification — classify sessions as `feature-rich` / `mixed` / `chore-only` based on vision_prs ratio; write `vision_prs` and `session_outcome` columns to metrics.md; `session_outcome=chore-only` triggers AMBER health signal in §4f (PR #655, 2026-04-20)
- ✅ `SM §4f`: silent-session detection — when session ends with 0 merged PRs AND 0 open PRs, increment `silent_session_count` in state.json; when streak ≥ 2 consecutive sessions, open `[NEEDS HUMAN: silent-session-streak]` issue with diagnosis guide; resets to 0 when any PR ships (PR #657, 2026-04-20)
- ✅ `docs/aide/metrics.md` schema: added `arch_convergence` and `sim_floor_delta` columns to batch log — tells the human whether the simulation is tracking reality; SM §4b [AI-STEP] updated to include these values (from sim-params.json) in new rows (PR #659, 2026-04-20)
- ✅ `SM §4c`: skill decay tracking — every 10 SM cycles, check each skill file age (via git log) against PROVENANCE.md mentions in last 90 days; if skill not mentioned in 90 days, flag as stale; posts informational report to report issue; does NOT auto-delete skills (PR #660, 2026-04-20)
- ✅ `SM §4f` batch report two-axis signal: report format leads with `progress: ADVANCING|STABLE|STALLED` + `health: GREEN|AMBER|RED`; ADVANCING=≥1 vision PR merged; STABLE=chores only shipped; STALLED=silent session (0 merged, 0 open); STABLE/STALLED upgrades GREEN→AMBER; exported as `SESSION_PROGRESS` env var; verbose details in `<details>` block (PR #625+#615, 2026-04-21)

- ✅ `SM §4c` / `agents/otherness.learn.md`: frame-lock break protocol — when arch_convergence >= 0.65 for 3 consecutive calibrations, SM §4c opens a `learn(arch): frame-lock` issue instructing the learn agent to choose an architecturally UNLIKE target (detected via skill category distribution from PROVENANCE.md); `otherness.learn.md §1b-arch-diverse` documents the "unlike" heuristic and category-to-search-terms mapping; flag stored as `frame_lock_detected` in state.json, cleared when convergence drops below 0.55 (PR #669, 2026-04-20)
- ✅ `agents/onboard.md` STEP 7b: post-run structural validation — after generating docs/aide/, runs inline checks equivalent to `scripts/check-onboarding.sh`; auto-fixes missing section headers, empty files, missing Stage/Journey markers; warns (does not block) on missing AGENTS.md fields or otherness-config.yaml sections; applies fixes as amended commit before PR creation (PR #670, 2026-04-20)
- ✅ `onboarding-existing-project.md` + `onboarding-new-project.md`: "First-run smoke test" section — 3 observable success signals (startup comment, feat/* branch, open PR); diagnosis commands for silent failure; common failure table (PR #671, 2026-04-20)

- ✅ `agents/onboard.md` STEP 4c: vision quality gate — after writing vision.md, checks: (1) ≥100 words, (2) named user/operator present, (3) specificity ratio < 35% generic filler words; for each failing check, agent revises inline via [AI-STEP] before proceeding to STEP 5 (PR #672, 2026-04-20)

- ✅ `/otherness.status` health dashboard: Step 0 added — shows 6 sections: health trend (last 5 batches), skills count + last learn date, queue depth + next item, journey status, simulation calibration + arch_convergence, reference project health; fits in ≤40 lines; graceful fallback for missing files (PR #673, 2026-04-20)

- ✅ `SM §4f` `docs/aide/progress.md` automated update: progress.md now includes "Last 3 batch outcomes" field from metrics.md session_outcome column; SM owns progress.md the same way it owns metrics.md — updated every batch via pull-rebase-retry push to main (PR #651, 2026-04-20)

- ✅ `SM §4e-iii` / `PM §5g`: simulation-behavior coupling gate — verifies that arch_convergence AMBER (≥0.7) signals triggered a learn session and that a completed learn session decreased arch_convergence in the next calibration; posts `[⚠️ Simulation loop unclosed]` on REPORT_ISSUE if the feedback chain never closed; informational only, does not block work (PR #668, 2026-04-21)

## Future (🔲)

- ✅ `coord.md §1c`: track guard-firing frequency in session metrics — `chore_only_guard_count` incremented in `state.json` on every guard fire; SM §4b reads, writes `queue_guard_fires` column to `metrics.md`, resets counter; `docs/aide/metrics.md` header row and metric definitions updated (PR #638, 2026-04-21)
- ✅ SM §4b housekeeping PR filter: `prs_merged` now counts only PRs with ≥1 changed file outside `docs/aide/` — housekeeping-only PRs (e.g. metrics.md updates) are excluded from the count; filter implemented in SM §4b via `gh pr view --json files`; fail-open: API error → PR counted as real; a session where all merged PRs are housekeeping correctly reports `prs_merged=0`, triggering AMBER via the existing `VISION_PRS==0` defect path (PR #720, 2026-04-21)
- ✅ COORD §1b unified startup signal reader: a new `§1b-preflight` block reads `housekeeping_streak`, `next_session_directive`, `frame_lock_detected`, `silent_session_count`, and open `needs-human` count at session start; applies them in explicit priority order (needs_human > streak≥3 > directive > frame_lock > none); sets `COORD_ACTION` env var; §1c consumes `COORD_ACTION=vision-first` to run vibe-vision-auto before queue-gen when streak≥3; consolidated log line per session shows all signal values and resolved action; graceful fallback: missing/malformed state.json → action=none, session continues normally (PR #757, 2026-04-21)
- ✅ Session type declared at session START: COORD §1b-session-type inspects top 3 queue items before any claim and writes `session_type_declared: feature-rich|mixed|chore-only` to `state.json`. If `chore-only`: sets `QUEUE_NEEDS_ENRICHMENT=true` so §1c fires enrichment guard before the first claim. Fail-open. (PR #775, 2026-04-21)
- ✅ Session minimum meaningful-PR contract: `standalone.md §1f-contract-level2` (inline vision synthesis when L1 recovery fails) and `§1f-contract-level3` (in-session `[DEFECT]` comment before SM/PM). Both guarded by session vars. Fail-open. (PR #796, 2026-04-21)
- ✅ Meaningful-PR rate is the primary throughput metric: SM §4f REPORT_BODY headline now shows "Shipped: N meaningful (N total)" — `meaningful_prs` leads, `prs_merged` is secondary in parentheses. `Vision PRs` and `Chores` moved to Details section. (PR #800, 2026-04-21)
- 🔲 Queue composition audit must run every 10 batches and post a plain-language summary: COORD §1c fires the chore-only guard reactively (when the queue is ALL chores). But the queue can be 80% chores and 20% features and the guard never fires — the session claims one feature item and leaves 19 chores unclaimed. Over time the chore backlog grows, the feature-to-chore ratio worsens, and the guard never detects the drift. SM §4a must, every 10 batches, scan all open `state=todo` issues in `state.json` and compute: `feature_items_count`, `chore_items_count`, `docs_items_count`, `ratio_feature: feature/(feature+chore+docs)`. If `ratio_feature < 0.3` (less than 30% of the queue is feature work): SM must open a `kind/chore priority/medium` issue "Queue composition: only N% feature items — chore accumulation detected. Run vibe-vision or manually close stale chore items." and post the ratio to the report issue. Without this audit, the chore-to-feature ratio can drift to 95%/5% while individual sessions remain technically "mixed" (not ALL chores), and the system never self-detects the creeping prioritisation failure. ⚠️ Inferred from reliability lens: the system produces housekeeping PRs because the queue composition drifts toward chores; no periodic audit exists to detect this drift before it becomes severe.
- 🔲 Recovery path effectiveness must be tracked — not just whether recovery was attempted: the session minimum meaningful-PR contract (above) and the existing same-session recovery path both specify that the loop must attempt multiple recovery levels before accepting a zero-PR exit. But no mechanism tracks whether those recovery attempts *work*. SM §4b must record three new fields in `state.json` per session: `recovery_attempted: bool`, `recovery_level_reached: 0|1|2|3` (0=no attempt, 1=second-chance item tried, 2=vision synthesis tried, 3=all levels exhausted), `recovery_succeeded: bool`. PM §5 stagnation check must flag when `recovery_attempted=true` AND `recovery_succeeded=false` for 2 consecutive sessions: this means the recovery paths themselves are broken, not just the primary path. A system that attempts recovery but always fails is worse than a system that never attempts it — it consumes session budget on broken recovery logic while producing the same zero-PR result. Without this tracking, the recovery contract is unverifiable: the agent can claim it attempted recovery without any observable evidence. ⚠️ Inferred from reliability lens: sessions fail silently because recovery mechanisms exist on paper but their effectiveness is untracked; the system cannot distinguish "recovery worked" from "recovery was bypassed" from "recovery was attempted but broken."
- 🔲 Claim-to-ENG-attempt rate must be tracked — COORD bail-out before ENG starts is a silent failure: there is a class of failure not yet detected by any existing mechanism: COORD claims an item (sets `state: in_progress`, creates a `feat/*` branch) but exits before ENG begins implementation — for example, when COORD reads the issue and decides the spec is ambiguous, or when the branch creation fails silently, or when COORD context-switches to SM/PM without completing the handoff to ENG. The current loop has no observable signal that distinguishes "COORD claimed, ENG attempted and produced zero-diff" (caught by zero-diff detection in doc 21) from "COORD claimed but ENG never started" (completely uncaught). The item stays `in_progress` in `state.json` and SM's stale-branch watchdog eventually reclaims it — but the session is counted as "productive" (it claimed an item). SM §4b must track `eng_attempts_per_session` — count of sessions where a `feat/*` branch was created AND at least one commit was pushed to it (proof ENG started). A session where `items_claimed > 0` but `eng_attempts == 0` means COORD claimed without ENG starting — write `eng_attempt_rate: 0` to metrics.md and flag with AMBER in next health comment. Without this metric, COORD bail-outs are invisible: the session claims items, the queue shows `in_progress`, and the health signal reports a "working" session that never implemented anything. ⚠️ Inferred from reliability lens: sessions still fail silently; the current zero-diff detection catches ENG finishing with no changes, but not ENG never starting; these are different failure modes with different root causes requiring different fixes.
- ✅ Simulation `recovery_action` stuck-loop detection: the simulation's `recovery_action` field in `sim-prediction.json` recommends one of four actions (`escalate_oldest_needs_human`, `prioritize_ci_fix`, `trigger_learn`, `trigger_vision_synthesis`). These actions are written by SM §4e after each calibration. But if the same `recovery_action` is recommended for 5 or more consecutive calibrations (every 10 batches = 50 batches), it is strong evidence the recovery action is being recommended but never executed, or is being executed but not working. The simulation is stuck recommending the same treatment while the patient doesn't improve. SM §4e must: (1) read the last 5 `recovery_action` values from `_state:sim-prediction-prev.json` history; (2) if all 5 are identical: open a `kind/bug priority/high` issue "[SIM STUCK] recovery_action=<action> repeated for 5 consecutive calibrations without measurable improvement — the recovery mechanism may be broken or the action may not be reaching the execution path in COORD"; (3) write `recovery_action_stuck: true` to `state.json`; COORD §1b-preflight must surface this in the preflight summary. A simulation that recommends the same recovery 50 batches in a row is not calibrating — it is oscillating. The loop must detect this and escalate before the 50 batches become 100. ⚠️ Inferred from honesty lens: the simulation exists but its predictions are not visibly changing agent behavior; a recovery_action that is repeated without any observable effect is the clearest possible evidence that the feedback loop between simulation and action is broken. (PR #680)
- 🔲 "Velocity without vision" detection — the loop ships PRs but none advance design doc items for >N batches: `meaningful_prs` (PRs referencing design doc numbers) is tracked per-batch, but there is no rolling-window check that detects when the system has been shipping real code (not housekeeping) that is nonetheless completely disconnected from design doc `🔲 Future` items. A system can have `meaningful_prs > 0` every batch (triggering GREEN) while all those PRs are architectural refactors, CI improvements, or SM mechanics — none closing a `🔲 → ✅` transition in any design doc. This is "velocity without vision": the loop is genuinely working, but the vision is not advancing. PM §5 must compute `vision_advancing_prs_20batch`: count of PRs in the last 20 batches that explicitly promoted a `🔲 Future` item to `✅ Present` (detectable by scanning design doc git history for `🔲` → `✅` line changes). If `vision_advancing_prs_20batch == 0` despite `meaningful_prs > 0` for the same window: PM must open a `kind/chore priority/high` issue "Velocity without vision: 20 batches of real work with zero design-doc item completions — the system is busy but not advancing the roadmap." Without this check, GREEN health for 20 consecutive batches can mask the fact that the roadmap has made zero progress. ⚠️ Inferred from reliability and honesty lenses: the loop is not honest enough; metrics capture delivery velocity but not whether the vision is actually advancing; a system that ships 20 batches without completing a single design doc Future item is spinning in place.
- 🔲 Vibe-vision scan-to-ship conversion rate must be tracked — the scan is not a forcing function if its outputs are never claimed: `vibe-vision-auto.md` SCAN 3 adds `🔲 ⚠️ Inferred` items to design docs every run. COORD creates issues from those items. But there is no first-class metric capturing the ratio of "scan-inferred items written" to "scan-inferred items eventually shipped." A scan that reliably produces 5 new Future items per run while 0 of them ever ship is not a forcing function — it is backlog generation. The scan is only justified if items it produces are eventually claimed and implemented. SM §4b must track `scan_inferred_items_shipped` per 20-batch window: count of PRs whose title or body references a `⚠️ Inferred` design doc item (detectable by scanning for `⚠️ Inferred` in recent PR bodies). When this count is 0 for 20 consecutive batches while `⚠️ Inferred` items are accumulating in design docs: SM §4a must open a `kind/chore priority/high` issue: "Scan inferred items not shipping: 20 batches with zero ⚠️ Inferred items claimed — vibe-vision is generating backlog but COORD is not claiming it. Review COORD §1b vision-pressure set build to ensure ⚠️ Inferred items are included in the priority boost." Without this metric, the vibe-vision scan can run indefinitely producing items that nobody ever claims, while the health signal stays GREEN because other metrics look fine. ⚠️ Inferred from all five pressure lenses: the system is not reliable enough (scans run but results don't ship), not honest enough (scan outputs accumulate without accountability), and not improving fast enough (if scan items don't ship, the self-improvement loop has a broken output stage).
- ✅ Minimum meaningful-PR contract exhaustion must trigger a human-visible escalation, not just a [DEFECT] comment: the session minimum meaningful-PR contract (PR #796) implements three recovery levels before accepting a zero-PR exit. Level 3 is a `[DEFECT]` comment posted to the report issue. But when the level-3 fallback is reached — meaning all recovery paths failed — the only output is an informational comment. There is no mechanism that escalates to `[NEEDS HUMAN]` or AMBER when level-3 exhaustion happens for 3 consecutive sessions. SM §4b must: (1) track `contract_exhausted_streak` in `state.json` — incremented when `recovery_level_reached=3` and `recovery_succeeded=false`; (2) when streak reaches 3: open a `[NEEDS HUMAN: persistent-recovery-failure]` issue: "The minimum meaningful-PR contract has been exhausted 3 consecutive sessions. All recovery levels (second-chance item, vision synthesis, inline claim) have failed. Root cause is not transient. Human investigation required."; (3) reset streak to 0 when any session ships a meaningful PR. Without this escalation, a broken recovery path can execute indefinitely (opening [DEFECT] comments every session) while GREEN health continues to be reported and the human has no signal that the system has permanently failed to ship real work. ⚠️ Inferred from reliability lens: a truly reliable system ships at least one meaningful PR every single run; when 3 consecutive sessions exhaust all recovery mechanisms, the system is fundamentally broken and must not self-declare GREEN. (PR #797)

- ✅ Fleet-level meaningful-PR rate must be monitored — otherness cannot call itself reliable if managed projects stall while self-reports look fine: SM §4b tracks meaningful-PR rate for the self-hosted otherness project, but `monitor.projects` lists managed projects (kardinal-promoter, kro-ui) whose sessions also fail silently. PM §5 already checks reference-project `_state` activity (Scenario 1) — but not whether those projects are shipping meaningful work, only that they ran. PM §5 must extend the reference-project health check: for each project in `monitor.projects`, compute `meaningful_prs_last_5d` (count merged PRs in last 5 days with `feat/` prefix in branch OR title containing `fix:` or `feat:`). If any managed project shows `meaningful_prs_last_5d == 0`: PM §5 must open a `kind/bug priority/high` issue: "[FLEET STALL] Managed project <repo> has shipped 0 meaningful PRs in 5 days — possible silent failure. Check: _state branch for batch history, queue depth, silent_session_count." Without this check, a managed project can spin for weeks in chore-only or zero-ship mode while otherness's own health signal stays GREEN. otherness cannot claim to be a reliable autonomous system if the projects it manages are silently failing and no automated signal surfaces it. ⚠️ Inferred from reliability lens: a truly reliable system ships at least one meaningful PR every single run without exception — but the current reliability tracking is self-referential; managed projects are monitored for liveness (_state activity) but not for delivery quality (meaningful PRs); a managed project that runs batches but ships only chores is indistinguishable from one that ships features based on the current PM §5 checks. (PR #797)

- ✅ `state.json` write atomicity — COORD marks items `in_progress` but the write can fail silently leaving ghost locks: COORD §1e claims an item by writing `state: in_progress` to `state.json` on the `_state` branch via `git push`. If the git push fails (network partition, branch divergence, stale credentials) after the local write but before the remote push confirms, the item is in `in_progress` state locally but the remote `_state` still shows `todo`. The next session reads `_state` (the remote), does not see the lock, and also claims the same item — two sessions attempt to implement the same issue concurrently. The stale-branch watchdog in SM §4a reclaims abandoned branches after 60 minutes, but the duplicate claim window (up to 60 minutes) can result in two concurrent feat/ branches for the same issue, a merge conflict on QA, and an `in_progress` ghost in the local state that never resolves. COORD §1e must verify the push succeeded by re-reading `_state` immediately after push: `gh api repos/$REPO/contents/.otherness/state.json?ref=_state | jq .content | base64 -d | python3 -c "..."` — confirm the item appears as `in_progress`. If the re-read shows `todo`: COORD must retry the push with rebase (max 2 retries) before claiming the item. If all retries fail: skip the item, log the failure, and exit cleanly (do not leave a ghost lock). Without this write confirmation, any transient git push failure creates a ghost lock that SM §4a only detects and clears after a 60-minute staleness window — a full session of productive capacity wasted on a state management failure that could have been caught in 5 seconds. ⚠️ Inferred from reliability lens: sessions fail silently; COORD bail-out before ENG starts (doc 21) and zero-diff detection (doc 21) both address symptoms of claim failures, but neither addresses the root cause of ghost locks created by unconfirmed state writes. (PR #793)

- 🔲 Per-session chore-output root cause must be logged, not just detected — "chore-only session" is a symptom, not a diagnosis: SM §4b detects `session_outcome=chore-only` and sets health to AMBER. But two completely different failure modes produce the same AMBER signal: (A) the queue was genuinely chore-heavy when the session started (queue-side cause — the fix is vibe-vision or queue audit), and (B) the queue had feature items available but COORD selected a chore anyway (COORD-side cause — the fix is priority logic). Without distinguishing these, the SM AMBER is actionless: the human and the system cannot determine whether to run vibe-vision (for A) or fix COORD's claim sort key (for B). COORD §1b-session-type must record alongside `session_type_declared` a second field: `chore_cause: queue_was_chore_heavy | coord_bypassed_feature_items | unknown`. Compute it at session start: if top 3 queue items include ≥1 `kind/enhancement` or `kind/bug` item AND the session ships only chores — the cause is `coord_bypassed_feature_items`. If the top 3 items are all `kind/chore` — the cause is `queue_was_chore_heavy`. SM §4b must write this to `metrics.md` as a `chore_cause` column when `session_outcome=chore-only`. PM §5 must distinguish the two patterns: if `coord_bypassed_feature_items` appears in 3 consecutive chore-only sessions, open a `kind/bug priority/high` issue: "COORD claim sort key is bypassing feature items — priority logic defect, not queue starvation." Without this split, the human receives an AMBER signal that is equally consistent with both failure modes and cannot determine the correct fix. ⚠️ Inferred from reliability lens: sessions produce housekeeping PRs with no real feature content; the current detection (AMBER on chore-only) correctly identifies the symptom but provides no causal information; the human cannot distinguish "the queue ran out of features" from "COORD chose chores despite features being available" — these require completely different interventions.

- 🔲 `docs/aide/roadmap.md` stage advancement must be tracked as a first-class health metric — the human cannot tell if the current stage is moving: `docs/aide/roadmap.md` describes Stages 11, 12 with specific deliverables and thresholds. But no mechanism tracks which stage deliverables have shipped vs. remain open, and the roadmap stages are not reflected in any health signal or metric. A human reading "Stage 11: Perpetual Vision Pressure" has no way to know whether any Stage 11 deliverable has shipped this week. PM §5 must: (1) parse `docs/aide/roadmap.md` to identify the current active stage (the first stage without "COMPLETE" status); (2) extract its deliverables list; (3) for each deliverable, check whether a corresponding `✅ Present` item exists in any design doc; (4) compute a `current_stage_completion_pct`: `present_count / total_deliverables * 100`; (5) write this to `state.json` as `current_stage_pct`; (6) include in the SM §4f health comment: "Stage 11: 2/6 deliverables (33%)". If `current_stage_pct` has not increased in 30 batches: PM §5 must open a `kind/chore priority/high` issue "Stage stall: Stage 11 progress frozen at N% for 30 batches — no stage deliverables have shipped. Review roadmap for blockers or misaligned queue." Without this metric, the roadmap stages are static documents. The health signal says GREEN while the human's actual question — "is the roadmap advancing?" — goes unanswered batch after batch. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell if the system is moving toward the vision; the roadmap exists but no metric tracks whether the active stage is advancing; a system that runs 100 batches on Stage 11 with 0 deliverables shipped is indistinguishable from one that shipped 5 of 6 based on the current health signal.

- 🔲 ENG pre-flight feature gate — ENG must verify the claimed issue represents a real behavioral change before opening a worktree: COORD claims an issue and hands off to ENG. ENG currently starts implementation immediately. But a significant subset of issues in the queue are `kind/chore`, `kind/docs`, or hygiene tasks that will produce only comment or whitespace changes — the "housekeeping PRs with no real feature content" failure mode. ENG §2a must add a pre-flight classification step before opening any worktree: (1) read the issue title and body; (2) classify as `behavioral` (changes agent instructions, scripts, or config that alters system behavior) or `non-behavioral` (changes docs, comments, metrics reporting format, whitespace); (3) if `non-behavioral` AND COORD has not explicitly set a `force_implementation: true` flag: post a `[ENG PRE-FLIGHT]` comment on the issue — "Classified as non-behavioral (docs/chore). Will proceed but this session's meaningful-PR count will not be incremented for this item." and set `IS_MEANINGFUL=false` in session vars; (4) when `IS_MEANINGFUL=false`: ENG still completes the work (does not abort) but the PR description must include "kind: non-behavioral — docs/chore only." SM §4b reads this flag and excludes the PR from `meaningful_prs`. Without this pre-flight gate, ENG spends session budget on non-behavioral work while counting it as meaningful — a root cause of housekeeping-only sessions that look like productive ones in the metrics. ⚠️ Inferred from reliability lens: sessions produce housekeeping PRs with no real feature content; the current filter (SM §4b housekeeping PR post-hoc exclusion) catches the output but does not prevent the session from spending its budget on non-meaningful work; the pre-flight gate moves the detection to before ENG starts, preserving session budget for behavioral work.

- 🔲 ENG must write a structured `eng_fail_reason` field to `state.json` when it exits without producing a PR — the next COORD session retries the same item blind: when ENG starts implementation on a claimed item but exits without opening a PR (CI failure, spec ambiguity, worktree error, time budget exceeded), it currently leaves no structured trace of WHY it failed. The item transitions from `in_progress` back to `todo` via SM §4a's stale-branch watchdog — but the watchdog only knows "stale", not "failed due to CI environment error" vs. "failed due to spec unclear". The next session's COORD claims the same item again and hits the same failure. This is the retry-same-failure loop that produces chains of identical silent sessions. ENG §2f exit block must write to `state.json`: `eng_fail_reason: ci_failure | spec_unclear | worktree_error | time_budget | unknown` and `eng_fail_at: <timestamp>`. COORD §1b-preflight must read `eng_fail_reason` for any item it is about to claim: if `eng_fail_reason: spec_unclear` → COORD must post a `[NEEDS HUMAN: spec-unclear]` comment on the issue before claiming it (asking the operator to clarify the spec); if `eng_fail_reason: ci_failure` → COORD must check whether CI is currently red before claiming; if `eng_fail_reason: worktree_error` → COORD must clean up the worktree directory before claiming. Without this field, every retry is a blind retry: the system cannot distinguish a transient failure (CI was flaky) from a structural one (spec cannot be implemented). Blind retries consume session budget, increment `failed_attempts`, and eventually hit the `failed_attempts >= 3` `blocked` label — but the root cause (knowable from the fail reason) was never surfaced. ⚠️ Inferred from reliability lens: sessions still fail silently; the current retry loop (stale-branch watchdog + claim priority) retries items without any context about why the previous attempt failed; the fail-reason field closes the loop between "item was attempted" and "item can be retried intelligently."

- 🔲 Queue-empty stall protocol — specific response when the queue empties AND enrichment produces no new items after 3 attempts: COORD §1c enrichment sequence (docs/design/22-queue-richness.md) specifies a 4-step fallback: design doc Future items → roadmap stage → vision synthesis → allow chore claim. Step 4 (allow chore claim) handles the case where enrichment finds something. But what happens when enrichment at all 4 levels produces nothing new AND the queue has zero todo items? Currently: undefined. The session either hangs on an empty queue or exits with 0 items claimed. SM §4b would detect a silent session but there would be no breadcrumb explaining it was a genuine queue-empty case (not ENG failure). COORD §1c must add a Queue-Empty Exit Protocol: when 3 consecutive enrichment attempts return 0 new items and the queue has 0 todo items: (1) post to the report issue: "[COORD] Queue exhausted — no unclaimed Future items found in docs/design/, roadmap, or vision synthesis. Session exiting cleanly (not a failure)."; (2) write `session_type: queue_empty` to `state.json`; (3) trigger a vibe-vision-auto inline scan (Step A equivalent) to generate new Future items before the next session; (4) exit without claiming. SM §4b must exclude `queue_empty` sessions from the `silent_session_count` — they are not failures, they are expected behavior during periods of rapid feature shipping. Without this protocol, a fully-shipped queue is indistinguishable from a silently-failing session — both produce 0 PRs, both increment `silent_session_count`, and both trigger the same AMBER health signal for opposite reasons (success vs. failure). ⚠️ Inferred from reliability lens: sessions stall when queues empty; the current enrichment sequence has a step-4 fallback for low-content queues but no defined behavior for fully-empty queues; the queue-empty exit protocol closes this gap with a clean observable signal.

---

## Zone 1 — Obligations

**O1 — Guard fires on chore-only queues, never on mixed queues.**
If ≥1 `kind/enhancement` or `kind/bug` item exists in the queue, no guard action
is taken. The guard is a quality floor, not a priority gate.

**O2 — Guard does not block sessions.**
If enrichment produces no new items, the guard logs and allows claiming the next
chore item. An indefinitely stalled session is worse than a chore-only session.

**O3 — Guard-triggered enrichment posts a comment on the report issue.**
The operator must be able to observe that the guard fired and what was injected.

---

## Zone 2 — Implementer's judgment

- Whether to re-use the existing `§1c` queue-gen Python block or add a separate
  guard check: preference is a separate, clearly-labeled guard block for readability.
- How to detect chore-only: check `labels` array in state.json features; items
  without labels are treated as `kind/enhancement` (unknown = not chore).

---

## Zone 3 — Scoped out

- Changing chore item priority relative to feature items (handled in §1e sort key)
- Auto-labeling issues that lack labels — separate hygiene concern
- Cross-session queue contamination analysis
