# 39: Human-Readable Health Dashboard — 30-Second System Status

> Status: Active | Created: 2026-04-20
> Applies to: otherness itself and all managed projects

---

## The problem

A human who opens the GitHub repo right now cannot quickly answer three questions:

1. Is the system healthy? (running, shipping, not stalled)
2. What did it ship today?
3. Is it moving toward the vision or circling in chores?

The data exists. `docs/aide/progress.md`, the report issue, `_state` branch commits,
and open PRs together contain the answers. But assembling them requires:
- opening the report issue (verbose, technical, multi-comment thread)
- reading `docs/aide/progress.md` (manually updated, often stale)
- running `/otherness.status` (requires local setup, not visible on GitHub)
- scanning open PRs (unlabelled, mixed signal)

The result: the human defaults to "I'll check later" and loses confidence in the
system. The system may be healthy and shipping but *look* idle to a casual observer.

This is not a tooling problem — it is a communication problem. The system must make
its health state legible without requiring any human tool invocation.

---

## What "fixed" looks like

A human opens the repo. In the top comment of the report issue (or the `docs/aide/progress.md`
file, pinned), they see a table like this — updated every batch, readable in 30 seconds:

```
## otherness health — 2026-04-20

| Signal        | Value        | Trend  |
|---|---|---|
| Health        | 🟢 GREEN     | stable |
| Batch         | #93          | +1     |
| Shipped today | 3 PRs        | ↑      |
| Vision PRs    | 2 / 3 (67%)  | good   |
| Queue         | 40 todo      | full   |
| Last PR       | feat(sm): §4f silent-session detection |
| Sim calibrated| 0 days ago   | fresh  |
| Needs human   | 0 open       | clean  |
```

This is the single-page health summary. It replaces the 20-line health comment with
a scannable table. It is the answer to "is it working?" in one glance.

---

## Present (✅)

*(Nothing shipped yet.)*

---

## Future (🔲)

- 🔲 39.0 — Visibility debt sprint: all 39.x items (39.1–39.19) have been `🔲 Future` since this doc was created. None are `✅ Present`. The visibility layer is fully specified but unimplemented after 90+ batches. This is not a specification problem — it is a prioritisation failure. The fix must be structural, not incremental: COORD §1c must execute one visibility sprint per every 5 batches until ≥3 of items 39.1–39.4 are `✅ Present`. A visibility sprint claims only 39.x issues until the sprint target is met or the session budget is exhausted. SM §4b must track `visibility_sprint_active: true/false` in `state.json` and include it in the health comment. When `visibility_sprint_active=false` AND ≥0 of 39.1–39.4 are Present AND >30 batches have elapsed: SM §4b must write `visibility_sprint_active: true` and open a `priority/critical kind/enhancement` issue: "[VISIBILITY SPRINT] All 39.1–39.4 items unshipped after 30 batches — forcing visibility sprint". The loop must self-diagnose this failure mode, not wait for the human to notice. ⚠️ Inferred from visibility lens: the entire visibility layer has been specified but 0 items have shipped; after 90+ batches the human still cannot answer the 3 core health questions; the root cause is that 39.x items compete against feature items and consistently lose; the fix requires a structural scheduling intervention, not just priority bumping.

- 🔲 39.1 — SM §4f: replace verbose health comment with structured table: the current health comment is a multi-line prose block (Health: GREEN, batch count, recent PRs). Replace it with a markdown table: rows for Health signal, Batch number, PRs merged today, Vision PR ratio, Queue depth, Last PR title, Sim calibrated age, Needs-human open count. The table must fit in 10 lines and be readable without context. Cap history: the report issue shows only the last table (edit the previous comment, don't add new ones). ⚠️ Inferred from visibility lens: report issue comments too verbose and technical; human cannot tell system health at a glance.
- 🔲 39.2 — SM §4f: edit the top-level report issue body with the latest health table: the report issue body (not a comment) serves as the persistent "current state" display. SM §4f must `gh issue edit $REPORT_ISSUE --body "$(cat <<'EOF'\n...\nEOF\n)"` to update the issue body with the latest health table every batch. A human clicking the report issue link sees current state immediately, without scrolling through comments. This is the "single page" — everything needed to assess health in one view.
- 🔲 39.3 — `docs/aide/progress.md` auto-sync with health table: the health table written by SM §4f to the report issue must also be written to `docs/aide/progress.md` in the same batch. The file is the human-readable record; the issue comment is the live signal. They must always agree. SM §4f writes one, commits it; the issue body is updated in the same step. No drift between the two.
- 🔲 39.4 — `/otherness.status` renders the same health table: the command currently outputs a custom format. It must render the exact same table that SM §4f writes to the report issue. Consistency: a human who runs `/otherness.status` sees the same signal they'd see opening the report issue. No two formats for the same data. ⚠️ Inferred from visibility lens: `/otherness.status` output format not standardised against the SM health comment.
- 🔲 39.5 — `_state` branch commit message includes health summary: every `_state` branch push by SM §4g must use the commit message format `"sm: batch N — GREEN | 3 PRs | 40 todo"`. This makes the `_state` branch commit history a lightweight timeline readable from `gh api` or the GitHub UI without opening any issue. Operators monitoring multiple projects can use `gh api repos/$REPO/commits?sha=_state&per_page=5` to get a 5-line health history in one API call. ⚠️ Inferred from visibility lens: no clean single-page health dashboard; _state branch not used as a timeline.
- 🔲 39.6 — Fleet health summary in `/otherness.status --fleet`: for operators running otherness on multiple projects, a single table showing all managed projects' health signals is needed. Each row: repo, health signal, batch number, last PR title, days since last activity. Data source: `_state` branch of each managed project. `/otherness.status --fleet` must render this table. Currently `/otherness.status` is single-project only; fleet visibility requires cross-repo `_state` reads. ⚠️ Inferred from visibility lens: operator managing multiple projects cannot see fleet health without opening each repo individually.
- 🔲 39.7 — Repo main page shows loop-alive signal without any clicks: a human landing on the GitHub repo root page currently sees no indication that the autonomous loop is alive. The README, repo description, and pinned files do not reflect real-time system state. The fix: SM §4g must update `docs/aide/progress.md` with a one-line status badge format compatible with GitHub's README rendering — e.g. a plain-text "Last batch: 2026-04-21 | Health: 🟢 | PRs today: 3" at the top of `progress.md`. Additionally, SM §4g must update the repo's GitHub description via `gh api repos/$REPO --method PATCH --field description="otherness | batch 94 | 🟢 GREEN | last PR: feat(sm)..."` (≤120 chars). A human arriving at the repo main page sees in the description field that the system ran recently and is healthy — without opening any file. ⚠️ Inferred from visibility lens: the system is healthy and shipping but looks idle to a casual observer; there is no zero-click health signal on the repo main page.
- 🔲 39.8 — Design doc Present-vs-reality auditor: SM §4f must verify that items marked ✅ Present in design docs actually exist in the agent instruction files. For each `✅ Present` item that names a specific agent instruction (e.g. "SM §4b: session outcome classification"), SM must check whether the agent file contains the described section header or instruction pattern. If not found: flag the item as `⚠️ Drift — marked Present but not found in agent files`. This is the minimum guard against the failure mode where a PR "implements" a design doc item by marking it ✅ without adding the actual agent instruction. The design doc says the loop is honest; the loop must prove it by verifying its own claims. ⚠️ Inferred from honesty lens: items are marked ✅ Present in design docs (e.g. session outcome classification PR #655) but the state.json and metrics rows show the implementation may not be running — the system has no mechanism to detect this gap between claim and reality.
- 🔲 39.9 — Health dashboard must be complete before the loop is considered "moving toward the vision": all seven 39.x items (39.1–39.7) are `🔲 Future` — none are `✅ Present`. A human opening the report issue today sees verbose unstructured comments, not a health table. A human visiting the repo main page sees no loop-alive signal. The `/otherness.status` command exists but outputs a non-standard format. The entire visibility layer is unimplemented while the loop has been running for 90+ batches. This is the highest-impact gap for human confidence in the system. COORD §1c must give `kind/enhancement` issues tagged `area/docs,area/tooling` that close 39.1–39.4 elevated priority over other queue items until ≥3 of them are `✅ Present`. The loop cannot be considered "moving toward the vision" while the operator cannot read its health at a glance. SM §4b must track a `visibility_prs` metric (PRs that close 39.x issues) and flag when this count is 0 for 5 consecutive batches while 39.x items remain open. ⚠️ Inferred from visibility lens: the visibility layer has been fully specified but none of it has shipped; after 90+ batches the human still cannot tell system health without manual investigation.
- 🔲 39.10 — SM §4f: write health summary to `$GITHUB_STEP_SUMMARY` on every batch: the Actions job summary page is visible to anyone with repo access, requires no local tools, and is indexed per-run. SM §4f must append the health table to `$GITHUB_STEP_SUMMARY` using `echo "..." >> $GITHUB_STEP_SUMMARY`. Format: the same markdown table as 39.1 (health, batch, PRs merged, vision ratio, queue depth, last PR, sim calibrated age, needs-human count). A human checking the Actions tab sees the health summary for that run directly in the job summary — no issue, no command, no file to open. This is the lowest-friction visibility mechanism available on GitHub and it requires exactly one `echo` line in the workflow. The fact that it has been omitted for 90+ batches while the visibility gap is called out as critical is itself an indicator of the gap. Cost: trivial. Value: a human checking "did the last run do anything useful?" gets an answer in 5 seconds. ⚠️ Inferred from visibility lens: zero-click health visibility from the Actions tab is not implemented; the most accessible GitHub visibility surface (job summary) is unused.
- 🔲 39.11 — All 39.x items have been `🔲 Future` for 90+ batches — COORD must prioritise them as a block: the visibility layer (39.1–39.10) has been fully specified across this design doc for over 90 batches. None are `✅ Present`. This means the system has run for 90+ batches without shipping any of the visibility improvements that the design doc identifies as the highest-impact gap for human confidence. This is a systemic prioritisation failure, not a specification gap. COORD §1c must: (1) detect when a design doc has ≥5 `🔲 Future` items that have been open (as GitHub issues) for >30 days without any being claimed; (2) when detected: create a `kind/enhancement priority/critical` issue: "[Visibility Debt] Design doc 39 has N unshipped items open for >30d — treating as technical debt sprint"; (3) assign `priority/critical` to all open 39.x issues; (4) set `next_session_directive: prioritize_doc_39` in `state.json`. The human wrote these items expecting them to ship. When they don't ship for 90 batches, the system must self-diagnose and self-escalate — not wait for the human to notice the staleness. ⚠️ Inferred from visibility lens: the entire visibility layer has been specified but none of it has shipped; the system does not self-detect long-running specification-without-implementation gaps.
- 🔲 39.12 — Visibility PRs must be verifiable: when a PR claims to implement a 39.x item (e.g. "feat(sm): structured health table in report issue"), QA must verify the claim by actually inspecting the report issue body. QA §3a currently checks code diffs and CI. It does NOT check GitHub artifacts (issue bodies, PR bodies, Actions summaries) to confirm that claimed visibility improvements are actually visible. QA must add a 39.x verification step: for any PR referencing `39.` in its title or body, QA must (1) `gh issue view $REPORT_ISSUE --json body`, (2) verify the issue body contains a markdown table with the expected columns, (3) fail the QA review if the table is absent. Without this check, a PR can claim to implement 39.1 by touching SM §4f code without the health table actually appearing in the report issue — the claim is unverified at merge time. ⚠️ Inferred from visibility lens: the visibility layer has been specified but no mechanism verifies that implementations are actually visible to a human visiting the GitHub UI.
- 🔲 39.13 — Health comment must name the most important PR shipped this session — not just a count: the proposed health table (39.1) includes a "Last PR" row, but the value populated there is the most-recent-by-time PR, not the highest-value PR. A session that ships 8 chore PRs and 1 feat PR will show the last chore in "Last PR" if it was committed after the feat. The human reads "Last PR: chore(sm): metrics update" and concludes nothing meaningful shipped. SM §4f must: (1) compute `best_pr_this_session` — the merged PR with the highest "vision value" (feat > fix > chore, tiebreak by design-doc-referenced > not); (2) use `best_pr_this_session.title` as the "Top PR" row in the health table, not the latest-by-time PR; (3) if `meaningful_prs == 0`: display "Top PR: none — chore-only session" in AMBER. This one field change converts the health table from "last thing committed" to "most important thing shipped" — which is what the human actually wants to know. The current design shows recency; this change shows value. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell what the system shipped today; the "Last PR" field in the proposed table will show a chore if the last commit was a chore, hiding the real signal about whether the system is moving toward the vision or spinning in circles.


- 🔲 39.14 — 30-second test must be the acceptance criterion for every visibility implementation: doc 39 defines the problem as "a human cannot quickly answer 3 questions." But none of the 39.x items define a concrete pass/fail criterion for that test. Each 39.x item that claims to improve visibility must include a "30-second test" — a specific script or sequence of actions a human performs, and the expected output in ≤30 seconds. Example for 39.1: "Human opens report issue → sees markdown table → reads health, batch, PRs, last PR in one scan — elapsed: ≤10 seconds." Example for 39.10: "Human opens Actions tab → clicks latest run → reads job summary table — elapsed: ≤15 seconds." QA §3a must verify the 30-second test for each 39.x PR before merging: run the test steps, confirm the output is present and readable in ≤30 seconds. Without an explicit acceptance criterion, visibility PRs can be merged that technically implement the feature but produce outputs that require 5 minutes to interpret (e.g. a 200-line health comment that technically contains a table, buried in prose). The 30-second test is not a performance test — it is a design constraint. Any visibility output that cannot be understood in 30 seconds is not a dashboard; it is a report. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly answer 3 questions; but the design docs for visibility improvements contain no acceptance criterion that actually tests the "quickly" requirement; implementations can pass QA review while still failing the user's actual need.
- 🔲 39.15 — `otherness-config.yaml` must accept an operator-written pressure context override: the vision pressure context is currently hardcoded in the scheduled workflow YAML — a file that requires a PR to change. A human operator who wants to redirect the system's focus (e.g. "focus on onboarding quality for the next 14 days, not self-improvement") must either edit the workflow YAML directly (risky, easy to break) or wait for vibe-vision-auto SCAN 5 to detect that the pressure is stale and add a rewrite item. Neither path is operator-friendly. `otherness-config.yaml` must accept an optional `vision.pressure_override: |` key — a multiline string that, when present, replaces the "Context for this vision scan:" block in the Step A workflow prompt at runtime. `vibe-vision-auto.md` SCAN 5 must check for this field at startup: if present and non-empty, use it instead of the workflow-embedded pressure block. The field is read-only to the agent — agents must never overwrite a human-provided `pressure_override`. This gives the human operator a single, safe, version-controlled location to redirect the autonomous loop without touching the agent files. The most dangerous gap in the current design is that the human cannot steer the system without making a CRITICAL-tier file change. `otherness-config.yaml` is LOW tier — safe for operator edits at any time. ⚠️ Inferred from visibility lens: the system is not honest enough because the human has no low-friction way to tell the system what to focus on; the pressure context is the primary steering mechanism but it is locked in a high-friction location; an operator who wants to raise the bar or change direction must either make a risky file edit or wait for the system to infer the change itself.
- 🔲 39.16 — Health comment must have a maximum byte length enforced by SM §4f: the current design (39.1) replaces verbose comments with a structured table. But as the system evolves, the table can grow — more rows added over time, `<details>` blocks expanding, stale entries accumulating. GitHub issue comments have no enforced display limit, but comments >10KB cause rendering slowdowns in the GitHub UI and comments >65KB are truncated silently by the API. SM §4f must enforce a hard cap of 4000 bytes on the health comment (report issue body). If the computed comment exceeds 4000 bytes: SM §4f must truncate the `<details>` verbose block first (it is context, not signal), then truncate the oldest batch history rows, preserving the latest health table and one-line footer. If after truncation the content is still >4000 bytes, post `[⚠️ Health comment overflow — see latest metrics.md for full batch history]` and a 5-row table only. Without this cap, the health comment grows unboundedly and the "fits in 10 lines" design constraint (39.1) will be violated within 50 batches as accumulated context is appended. ⚠️ Inferred from visibility lens: the design specifies a 10-line health table but no mechanism enforces the length constraint over time; the comment will grow as the system adds more context to each batch report.
- 🔲 39.17 — Report issue comment thread must be pruned to the last 10 SM comments: every SM batch posts at least one comment on the report issue. After 90+ batches, the comment thread has 90+ entries. A human opening the report issue must scroll through 90 historical health comments to find the current state. 39.2 (edit the issue BODY) addresses the body — but the comment thread is not addressed. SM §4g must, every 10 batches, delete report issue comments posted by `otherness[bot]` that are older than the last 10 SM comments (using `gh api repos/$REPO/issues/$REPORT_ISSUE/comments --jq 'sort_by(.created_at) | .[:-10] | .[].id'` and then `gh api --method DELETE repos/$REPO/issues/comments/{id}`). The result: the comment thread always shows the last 10 batch summaries — enough historical context without the thread being a 90-entry scroll. Without this pruning, the report issue degrades from a health dashboard into a scroll-intensive log file that requires navigating past 90 stale comments to reach the current signal. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell if the system is healthy; the report issue comment thread is hundreds of entries long; no mechanism removes stale health comments to keep the dashboard readable.
- 🔲 39.18 — Health signal staleness detection: SM §4g session-branch merge failure leaves health signal frozen at prior batch: SM §4f writes the health table to the report issue body and SM §4g merges the session branch — but these are two separate operations. If SM §4g fails (branch conflict, CI red on the session branch, API timeout), the health table was written but the session's work was not merged. Worse: the next session starts, SM §4f writes a new health table showing the CURRENT batch as GREEN (because the SM step ran) — while the actual code changes from both the failed batch and the new batch sit on unmerged branches. The health signal is factually inaccurate: it shows GREEN because SM ran successfully, but the product has not received any shipped code in two batches. SM §4g must: (1) after attempting session branch merge, verify the session branch is absent from the remote (confirming the merge completed); (2) if the branch still exists after merge attempt: write `session_branch_merge_failed: true` to `_state`, set health to AMBER, and include in the health comment: "⚠️ AMBER — session branch merge incomplete; last N batches may not be on main." Without this check, the health signal GREEN means "SM ran" not "code shipped" — the same dishonesty gap that meaningful_prs vs prs_merged addresses for individual PRs, but at the session-branch level. ⚠️ Inferred from visibility lens: a human looking at GitHub cannot quickly tell if the system is healthy; the health signal currently reflects whether SM §4f executed, not whether the session's work actually landed on main; a failed SM §4g produces a false GREEN.
- 🔲 39.19 — Health signal must include "last real code shipped" timestamp, not just batch number: the proposed health table (39.1) includes "Batch" and "Last PR" — but "Batch N" tells the human when SM §4f ran, and "Last PR" tells them the most recent merged PR title. Neither tells them when code actually last landed on the main branch (which may differ if recent "batches" were SM-only or chore-only). SM §4f must add a `last_code_shipped_at` field to the health table: timestamp of the most recently merged PR that changed at least one file outside `{docs/aide/, .otherness/}`. This timestamp is stored in `state.json` as `last_code_shipped_at` and updated whenever a PR meeting the criterion merges. A health table that shows "Batch 150 — Last code: 8 days ago" immediately tells the human the loop is spinning (running batches) without shipping (changing code). Without this field, a human reading "Batch 150 | 🟢 GREEN" has no way to know whether any real code has shipped in the last 8 batches — they would need to read 8 batch PRs to find out. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell whether the system is moving toward the vision or spinning in circles; the batch counter advances regardless of whether real code is shipping.
- 🔲 39.20 — `docs/aide/progress.md` SM-cycle counter is stale and must be auto-corrected: `docs/aide/progress.md` shows "SM cycle: 6" (as of 2026-04-20). The actual SM cycle count is now substantially higher. This specific field is one concrete symptom of the broader progress.md staleness gap (39.3), but it has an additional property: a human reading "SM cycle: 6" while the batch count is 100+ concludes either that SM cycles are a different unit from batches (confusing) or that progress.md has not been updated in a long time (loss of trust). SM §4f must: (1) read the current batch count from `docs/aide/metrics.md`; (2) write `SM cycle: N` where N = total rows in metrics.md (same as batch count); (3) write the agent version hash from `git -C ~/.otherness rev-parse --short HEAD` as "Agent: otherness@<hash>" replacing any stale hash. These two fields must always match the current session's values. A progress.md that says "SM cycle: 6 | batch 101" is a contradiction that undermines confidence in all other data in the file. ⚠️ Inferred from visibility lens: a human looking at docs/aide/progress.md cannot tell if the system is healthy; the SM cycle field is frozen at 6 while the real batch count has advanced to 100+; this is an observable, specific staleness defect that causes immediate loss of trust in the system's self-reporting.

- 🔲 39.23 — All `docs/aide/` files must be verified fresh on every batch and a staleness alert posted if any are >5 batches behind: `docs/aide/progress.md`, `docs/aide/metrics.md`, and `docs/aide/roadmap.md` are supposed to be maintained by SM §4f and PM §5 automatically. But if those agents skip their write step (LLM token limit, API failure, partial session exit), the files drift silently. A human opening `docs/aide/progress.md` today sees "SM cycle: 6" because SM's automated update never ran after that batch. There is no mechanism that detects when *any* docs/aide/ file has not been updated in the expected timeframe. SM §4a must perform a docs-freshness check at session start: for each of `docs/aide/progress.md`, `docs/aide/metrics.md`, use `git log --follow --format=%ci -1 -- <file>` to get the last commit date; compute the number of batches since that commit (using `metrics.md` row count as the batch clock); if any file is more than 5 batches old: post a `[⚠️ Docs freshness gap]` comment on the report issue listing each stale file and its last-commit batch number. If `docs/aide/progress.md` is stale: SM §4f must regenerate it in the current session regardless of whether other work is pending. The docs-freshness check converts silently stale files into a visible, actionable signal. Without it, the human arrives at `docs/aide/` and cannot tell whether the data is current or months old — destroying confidence in all the self-reporting the system does. This is the root cause of the "docs/aide/progress.md says SM cycle: 6" visible defect in the pressure context. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell if the system is healthy; `docs/aide/` files are the primary human-readable record; they are stale because SM's write steps fail silently with no detection; the fix is a freshness check that makes staleness visible within one session of its occurrence.

- 🔲 39.21 — First-time human arrival experience: a new team member who opens the GitHub repo today cannot orient in under 5 minutes without reading many files: all 20 visibility items in this doc address the signals available to someone who already knows what otherness is, what the report issue is, what `/otherness.status` does. But a new team member (developer, reviewer, or stakeholder) arriving at the repo for the first time encounters: a `README.md` that describes what otherness is (future PR will keep it fresh) but gives no indication that the loop is currently running, what it shipped last, or how to check its health. There is no "Getting Started as an Observer" section. `README.md` must include a minimal "Current Status" section — generated by SM §4g every batch — containing three lines: (1) Loop status: RUNNING / STALLED (based on `_state` activity in last 72h), (2) Last meaningful PR: `<title>` merged `<N> days ago`, (3) How to check health: `gh issue view <REPORT_ISSUE> --repo <REPO>`. This section replaces any manually-written status paragraph. SM §4g must generate and commit this section in the same step it pushes the `_state` batch state. A first-time visitor who opens `README.md` knows in 10 seconds whether the system is alive and what to read next. Without this section, README.md is a description of what otherness should do — not evidence that it is doing it. ⚠️ Inferred from visibility lens: a human looking at GitHub right now cannot quickly tell if the system is healthy; the README is the first file a new observer reads; it currently contains no live status signal; fixing this is a one-paragraph SM §4g addition that requires no new infrastructure.

- 🔲 39.22 — Report issue comment de-duplication: SM posts at minimum one comment per batch, but the report issue has no deduplication guard: when SM §4f posts the health comment AND a subsequent step also posts (e.g. the `[VIBE-VISION-AUTO]` comment from the Step A scan), the report issue receives 2–4 comments per batch. After 100 batches, the thread has 200–400 comments. Item 39.17 specifies pruning old SM comments to keep the last 10 — but it does not address the within-batch duplication where multiple agents post separate comments in the same batch session. SM §4f must become the SOLE writer of the per-batch health comment: all other agents (vibe-vision-auto, COORD queue-gen, QA merge notices) that currently post to the report issue must instead write their status to `state.json` under a `batch_log: []` array. SM §4f reads `batch_log` at the end of each batch and includes those entries as a collapsible `<details>` block in the single health comment. One comment per batch. This is not a cosmetic change — it is the precondition for item 39.17 (pruning) to produce a clean thread with exactly 10 visible entries. Without this consolidation, pruning the last 10 SM comments still leaves behind all the non-SM comments (vision scan comments, coord comments), making the "10 visible entries" goal unachievable. ⚠️ Inferred from visibility lens: the report issue comment thread is too verbose and technical; the root cause is not the length of individual comments but the volume of separate comments from multiple agents; consolidation into one SM-owned comment per batch is the fix.
SM §4f is the only writer. The table must reflect the batch that just completed —
not the previous batch, not a cached value. Stale health data is worse than no data.

**O2 — The table uses emoji sparingly and consistently.**
Green circle = GREEN, yellow = AMBER, red = RED. No other emoji. The table must be
readable in terminal output (ASCII fallback) and GitHub markdown. Do not use emoji
that render incorrectly in terminal.

**O3 — The report issue body is the authoritative health display.**
PR comments, commit messages, and `/otherness.status` are derived signals. When any
of them disagree with the report issue body, the report issue body wins. SM §4f updates
the report issue body as the primary write, then derives other outputs from it.

**O4 — The health table never includes personally identifiable information.**
No usernames, no API keys, no internal URLs. The table is a public-facing signal.

---

## Zone 2 — Implementer's judgment

- The report issue body edit: `gh issue edit $REPORT_ISSUE --repo $REPO --body "..."` is the
  correct command. The body should include the table plus a one-line footer: "Updated by
  SM §4f — batch $N — $DATE". This footer is the timestamp.
- `/otherness.status` output: render the same table to stdout, optionally followed by the
  last 3 lines of the health comment thread for context.
- `_state` commit message format: keep it to <72 chars. "sm: batch N — GREEN | M PRs | K todo"
  is 40–50 chars and fits. Do not include PR titles (too long, breaks the format).
- Fleet mode: read `monitor.projects` from `otherness-config.yaml` for the project list.
  Each project's `_state` branch is readable without authentication on public repos.

---

## Zone 3 — Scoped out

- External dashboard (Grafana, Datadog, custom web UI) — GitHub UI is sufficient for now
- Real-time streaming health (batch granularity is the right frequency)
- Slack/email notifications (out of scope until user request)
- Mobile-optimised health view
