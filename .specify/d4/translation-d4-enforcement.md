[📋 D4 TRANSLATION] — 2026-04-17

Heard:     "all guardrails possible, to the local level before git, agents not allowed to
            modify code if not through otherness.run, docs not through vibe-vision, any
            other attempt gets deflected and redirects to the right command"

Intent:    D4 enforcement at every layer — pre-execution mode gates, not just CI.
           The guard fires when the agent reads instructions, before any file is touched.
           Wrong-mode sessions produce a redirect, not implementation output.

D4 layer:  New design doc — docs/design/07-d4-enforcement.md

Proceeding after 60s.
